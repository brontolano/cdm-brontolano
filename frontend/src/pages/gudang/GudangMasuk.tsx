import { MobilePage } from '../lapangan/shell';

// Stub — diimplementasikan penuh di issue #18 (barang masuk + foto nota + stok/HPP).
export default function GudangMasuk() {
  return (
    <MobilePage title="Barang Masuk" back="/gudang" color="#c2410c">
      <p style={{ color: '#64748b' }}>Input barang masuk + foto nota sedang disiapkan (#18).</p>
    </MobilePage>
  );
}
