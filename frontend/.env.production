# Build produksi: API satu-origin lewat proxy nginx (/api → backend:4000).
# Vite memuat .env.production lebih dulu daripada .env saat `vite build`,
# sehingga nilai ini mengalahkan VITE_API_URL=http://localhost:4000/api di .env.
VITE_API_URL=/api
VITE_WA_ORDER_NUMBER=628557271197
